# 项目协作规则

## 记录要求

- 变更记录、需求记录、项目知识以及其他需要留档的项目事实统一写入 `docs/change-record.md`。
- 每次改动代码或记录用户需求，都要同步更新 `docs/change-record.md`。
- `docs/change-record.md` 中的内容必须有明确依据，依据可以来自代码、日志、配置、数据库、截图、真实数据、官方文档或 Codex CLI 官方源码。
- 禁止把猜测写入 `docs/change-record.md`。无法确认时先标记为待验证，或增加诊断日志后等待用户重新发版复现。
- 发现之前写入 `docs/change-record.md` 的内容存在错误、依据不足或结论过度，可以直接修正。
- 文档更新保持简洁，避免流水账和重复表述。纯文本统一记录到 `docs/change-record.md`
## 排查要求

- 不凭空推测问题原因。优先查阅相关前后端代码、日志、配置、数据库、截图、真实请求数据和 Codex CLI 官方源码。
- 如果仍无法确认问题原因，优先补充最小诊断日志，让用户重新构建部署后再根据日志继续排查。
- 用户提供本地图片路径时，必须读取图片后再分析。

## 代码清理要求

- 项目源码中存在大量废弃和兼容代码，修复问题时尽量保持源码干净。
- 对已经确认不会生效、没有业务价值或只会掩盖问题的废弃兼容逻辑和兜底逻辑，可以在本次相关范围内删除。
- 不确定某段逻辑是否仍需保留时，先向用户确认“这个逻辑需要保留吗？”再删除。
- 非业务明确要求，不做无关重构，不扩大改动范围。
